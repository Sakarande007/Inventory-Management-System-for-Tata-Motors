<UserControl x:Class="PaintShopIMS.Views.Reports.ReportsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" Background="Transparent"
             d:DesignHeight="600" d:DesignWidth="1000">
             
    <UserControl.Resources>
        <!-- Type Pill Template -->
        <DataTemplate x:Key="TypeReportTemplate">
            <Border CornerRadius="4" Padding="8,2" HorizontalAlignment="Center">
                <Border.Style>
                    <Style TargetType="Border">
                        <Setter Property="Background" Value="#EAF3DE"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding Type}" Value="OUTWARD">
                                <Setter Property="Background" Value="#FAEEDA"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Border.Style>
                <TextBlock Text="{Binding Type}" FontSize="{StaticResource CaptionSize}" FontWeight="Bold">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Foreground" Value="{StaticResource SuccessBrush}"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Type}" Value="OUTWARD">
                                    <Setter Property="Foreground" Value="#E65100"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
            </Border>
        </DataTemplate>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Transaction Reports" Style="{StaticResource PageTitleText}" Grid.Row="0" Margin="0,0,0,20"/>

        <!-- Filter Card -->
        <Border Grid.Row="1" Style="{StaticResource CardBorder}" Margin="0,0,0,15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="From:" Style="{StaticResource ModernTextBlock}" VerticalAlignment="Center" Margin="0,0,10,0"/>
                    <DatePicker SelectedDate="{Binding FromDate, UpdateSourceTrigger=PropertyChanged}" Width="150" Margin="0,0,20,0"/>
                    
                    <TextBlock Text="To:" Style="{StaticResource ModernTextBlock}" VerticalAlignment="Center" Margin="0,0,10,0"/>
                    <DatePicker SelectedDate="{Binding ToDate, UpdateSourceTrigger=PropertyChanged}" Width="150"/>
                </StackPanel>

                <StackPanel Grid.Column="2" Orientation="Horizontal">
                    <Button Content="Generate Report" Command="{Binding GenerateReportCommand}" Style="{StaticResource PrimaryButton}" Margin="0,0,10,0" Width="140"/>
                    <Button Content="Export to Excel" Command="{Binding ExportToExcelCommand}" Foreground="#2E7D32" BorderBrush="#2E7D32" Style="{StaticResource GhostButton}" Width="130"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Summary Bar -->
        <Border Grid.Row="2" Background="#E2E8F0" CornerRadius="4" Padding="15,10" Margin="0,0,0,15">
            <StackPanel Orientation="Horizontal">
                <TextBlock Foreground="{StaticResource TextPrimaryBrush}" FontSize="{StaticResource SectionSize}" FontWeight="SemiBold">
                    <Run Text="Total: "/>
                    <Run Text="{Binding TotalCount}"/>
                    <Run Text="  |  Inward: "/>
                    <Run Text="{Binding InwardCount}"/>
                    <Run Text="  |  Outward: "/>
                    <Run Text="{Binding OutwardCount}"/>
                </TextBlock>
                <ProgressBar IsIndeterminate="True" Width="200" Height="4" Margin="30,0,0,0" Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}}"/>
            </StackPanel>
        </Border>

        <!-- Data Grid -->
        <Border Grid.Row="3" Style="{StaticResource CardBorder}">
            <DataGrid ItemsSource="{Binding ReportData}" Style="{StaticResource ModernDataGrid}">
                <DataGrid.Columns>
                    <DataGridTemplateColumn Header="Type" CellTemplate="{StaticResource TypeReportTemplate}" Width="100"/>
                    <DataGridTextColumn Header="Part No" Binding="{Binding PartNo}" Width="150"/>
                    <DataGridTextColumn Header="Serial Number" Binding="{Binding SerialNumber}" Width="200"/>
                    <DataGridTextColumn Header="Rework Code" Binding="{Binding DefectCode}" Width="120"/>
                    <DataGridTextColumn Header="Scan Date" Binding="{Binding ScanDate}" Width="160"/>
                    <DataGridTextColumn Header="Scanned By" Binding="{Binding ScannedBy}" Width="150"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>
