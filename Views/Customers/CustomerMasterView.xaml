<UserControl x:Class="PaintShopIMS.Views.Customers.CustomerMasterView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" Background="Transparent"
             d:DesignHeight="600" d:DesignWidth="800">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Customer Master" Style="{StaticResource PageTitleText}" Grid.Row="0" Margin="0,0,0,20"/>

        <!-- Toolbar row -->
        <Border Grid.Row="1" Style="{StaticResource CardBorder}" Margin="0,0,0,15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="300"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Search -->
                <TextBox Grid.Column="0" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ModernTextBox}" Tag="Search customers..." Height="36"/>

                <!-- Action Buttons -->
                <StackPanel Grid.Column="2" Orientation="Horizontal">
                    <Button Content="Add Customer" Command="{Binding AddCustomerCommand}" Style="{StaticResource PrimaryButton}" Margin="0,0,10,0"/>
                    <Button Content="Edit" Command="{Binding EditCustomerCommand}" Style="{StaticResource GhostButton}" Margin="0,0,10,0"/>
                    <Button Content="Delete" Command="{Binding DeleteCustomerCommand}" Style="{StaticResource DangerButton}"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Data Grid -->
        <Border Grid.Row="2" Style="{StaticResource CardBorder}">
            <DataGrid ItemsSource="{Binding FilteredCustomers}" SelectedItem="{Binding SelectedCustomer}" 
                      Style="{StaticResource ModernDataGrid}" MouseDoubleClick="DataGrid_MouseDoubleClick" SelectionMode="Single">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Customer ID" Binding="{Binding CustomerId}" Width="100"/>
                    <DataGridTextColumn Header="Customer Name" Binding="{Binding CustomerName}" Width="250"/>
                    <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="300"/>
                    <DataGridTextColumn Header="Remark" Binding="{Binding Remark}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <!-- Status bar -->
        <TextBlock Grid.Row="3" FontSize="{StaticResource CaptionSize}" Foreground="{StaticResource TextMutedBrush}" Margin="0,10,0,0">
            <Run Text="{Binding CustomersCount, Mode=OneWay}"/>
            <Run Text=" customers total"/>
        </TextBlock>
    </Grid>
</UserControl>
